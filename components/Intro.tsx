import React from 'react'
import styles from "@/styles/Intro.module.css"

type IntroProps = {
	badgeText: string,
	headerText: string,
	textContent: string
}

const Detail = (props: IntroProps) => {

  return (
  	<div className={styles.introCont}>
  		
  	</div>
  )
}


export default Detail;